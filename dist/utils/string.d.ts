/**
 * Replace all occurrences of a string with another string
 *
 * @param data input string
 * @param search string to search for
 * @param replacement string to replace with
 */
export declare function replaceAll(data: string, search: string, replacement: string): string;
/**
 * Generate random base64 string with passed length
 */
export declare function generateRandomBase64String(length?: number): string;
